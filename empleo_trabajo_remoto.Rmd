---
title: "proyecto_ml"
author: "diego"
date: "2023-09-23"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)

library(readr)
library(cluster)
library(dplyr)





```


```{r}
library(readr)
Empleo_AMBA_a_letra_1_ <- read_csv("datos/Empleo-AMBA-a-letra (1).csv")
```

```{r}
head(Empleo_AMBA_a_letra_1_)
```


```{r}
actividades <- read_csv("datos/actividades.csv")
```

```{r}
head(actividades)
```

```{r}
jurisdicciones <- read_csv("datos/jurisdicciones.csv")
```

```{r}
Empleo_AMBA_sin_desagregar <- read_csv("datos/Empleo-AMBA-sin-desagregar.csv")
```

```{r}
dim(Empleo_AMBA_sin_desagregar)
ncol(Empleo_AMBA_sin_desagregar)
nrow(Empleo_AMBA_sin_desagregar)
```

```{r}
attr(Empleo_AMBA_sin_desagregar, "names")
attr(Empleo_AMBA_sin_desagregar, "class")
```

```{r}
min(Empleo_AMBA_sin_desagregar$Remuneracion_media_Hombres)
max(Empleo_AMBA_sin_desagregar$Remuneracion_media_Hombres)
```

```{r}
min(Empleo_AMBA_sin_desagregar$Remuneracion_media_Mujeres, na.rm = TRUE)
max(Empleo_AMBA_sin_desagregar$Remuneracion_media_Mujeres, na.rm = TRUE)

```






```{r}
hist(Empleo_AMBA_sin_desagregar$Remuneracion_media_Hombres, breaks = 1000,
     col='blue',
     main='histograma salario_medio',
     xlab='salario',
     ylab='Frequency')

```


```{r}
empleo <- Empleo_AMBA_sin_desagregar
head(empleo)
```

```{r}
summary(empleo)
```

```{r}
empleo_tipo <- Empleo_AMBA_a_letra_1_
head(empleo_tipo)
```

```{r}
summary(empleo_tipo)
```

```{r}
empleo_means <- aggregate(empleo_tipo$Remuneracion_media,    
                        list(empleo_tipo$letra),
                        mean)
empleo_means
```


```{r}
#boxplots comparados
boxplot(filtrado2$Remuneracion_media_Mujeres ~ filtrado2$letra,    
        col = 1:3)
points(x = 1:nrow(empleo_means),
       y = empleo_means$x,
       pch = 16,
       col = "white")
text(x = 1:nrow(empleo_means),
     y = empleo_means$x + 1.2,
     labels = round(empleo_means$x, 2),
     col = "white")
```















```{r}
plot(empleo_tipo$Remuneracion_media_Hombres, empleo_tipo$Remuneracion_media_Mujeres,
     col='steelblue',
     main='Scatterplot',
     xlab='Media hombres',
     ylab='Media mujeres',
     pch=19)

```

### En este gráfico hay dos datos que spbresalen del resto y que son representaciones salariales que no se corresponden con la realidad... esto impide tener una apreciación gráfica de las restantes correspondencias
### se imponen parámetros para que la representación refleja mejor la información, filtrando aquellos datos que resultan inconsistentes con la muestra

```{r}
filtrado <- empleo_tipo|>
  filter(Remuneracion_media_Hombres < 3000000, Remuneracion_media_Mujeres < 2000000)

ggplot(filtrado, aes(Remuneracion_media_Hombres,
                        Remuneracion_media_Mujeres)
       ) + geom_point(aes(col=letra), size=3)
```


```{r}
filtrado2 <- empleo_tipo|>
  filter(Remuneracion_media_Hombres < 500000, Remuneracion_media_Mujeres < 500000)

ggplot(filtrado2, aes(Remuneracion_media_Hombres,
                        Remuneracion_media_Mujeres)
       ) + geom_point(aes(col=letra), size=3)
```














